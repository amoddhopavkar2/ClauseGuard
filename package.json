{
  "name": "clauseguard",
  "version": "1.0.0",
  "description": "Detects and highlights risky clauses in Terms of Service and Privacy Policy pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
